*gRPCer* is a library with some helper functions for calling a gRPC endpoint in Go,
and a `protoc` plugin for generating a helper lib for easier calling those endpoints:
[./protoc-gen-grpcer](protoc-gen-grpcer)
